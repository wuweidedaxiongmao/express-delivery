<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.StudentMapper">
    <select id="selectAll" resultType="com.example.springboot.entity.Student">
        SELECT * FROM student
            <where>
                <if test="name != null">
                    name like concat('%',#{name},'%')
                </if>
            </where>
            order by id asc
    </select>
    
    <insert id="insert" parameterType="com.example.springboot.entity.Student">
        INSERT INTO `student` (`username`,`password`,`name`,`avatar`,`phone`,`email`,`role`,`level`,`rating`,`order_count`,`created_at`,`updated_at`)
        VALUES (#{username},#{password},#{name},#{avatar},#{phone},#{email},#{role},#{level},#{rating},#{orderCount},#{createdAt},#{updatedAt})
    </insert>
    
    <update id="updateById" parameterType="com.example.springboot.entity.Student">
        update `student` set username=#{username},password=#{password},name=#{name},avatar=#{avatar},phone=#{phone},email=#{email},role=#{role},level=#{level},rating=#{rating},order_count=#{orderCount},created_at=#{createdAt},updated_at=#{updatedAt}
        where id=#{id}
    </update>
</mapper>